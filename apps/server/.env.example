# Copy this file to `.env` in the same folder to set local defaults
# These only apply to the backend process (npm run dev) and do not
# pollute your shell environment.

# Parsing / LSP
#ORAICLE_ENABLE_LSP=on
#ORAICLE_LS_INIT=on
#ORAICLE_TS_ENRICH_IMPORTS=off

# Indexing performance
# Use all but one core, capped at 8 by default. Override if you want.
INDEX_CONCURRENCY=8

# Embedding throughput
EMBED_BATCH_SIZE=256

# Resolver behavior (keep off unless you explicitly test workers)
#INDEX_USE_WORKERS=off

# Respect global gitignore (set to off if you don't want ~/.config/git/ignore)
#ORAICLE_RESPECT_GLOBAL_GITIGNORE=on

# Sidecar default (only used if not set in package.json script)
CODE_INDEXER_EMBEDDINGS_ENGINE=python-sidecar
SIDE_CAR_BASE_URL=http://127.0.0.1:8088

